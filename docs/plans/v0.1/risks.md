# Risks — jaewon-plugin-learning v0.1

<!-- scope: risks + mitigations + owners + probability -->
<!-- deps: overview.md, phase-8-smoke-test.md, phase-0.5-real-llm-spike.md -->

## Register

| ID | Risk | Phase(s) exposed | Prob | Impact | Mitigation | Owner |
|----|------|------------------|:----:|:------:|------------|-------|
| R1 | **CLAUDE.md drift** — main session accumulates learner-specific rules over time, defeating the "never edit CLAUDE.md" invariant. | 5, 6 | MED | HIGH | Lint pass in `wiki-lint` skill + SessionStart hook that reads `wiki/learner/push-tactics.md` as source of truth; explicit phase-5.4 rule that `learn` skill contains zero personalization logic. | planner (spec) / user (review) |
| R2 | **Two-repo confusion** — user edits files in plugin repo thinking they are in wiki fork; learner state gets overwritten on plugin update. | 1, 8 | HIGH | HIGH | `wiki-template/CLAUDE.md` section "What never to edit"; `setup-learning-wiki` skill runs a CWD-is-fork check (phase-5.2 step 2); plugin repo has zero files under `wiki/`, `.jaewon-learning/`, `dashboard/`. | planner / executor |
| R3 | **Aggressive crawl latency + cost** — researcher's WebSearch+WebFetch+arxiv+YouTube can take >10min per course, blowing spec §12 "full cycle <30 min". | 3, 4 | HIGH | MED | **REV 2**: new MCP tool `learning_crawl_guard` (phase-3.10/3.11) enforces hard limits: max_sources=10, max_elapsed_ms=300000 (5 min), max_tokens=50000; fails closed on any error; wired into researcher's mandatory pre-fetch check (phase-4.2). Researcher prompt still caps breadth as secondary defense. Flagged in notes.md. | executor |
| R4 | **Schema drift between agent output and schemas/** — evaluator LLM output fails validation repeatedly, blocking verdict. | 3, 4, 5 | MED | HIGH | **REV 2**: new phase 0.5 real-LLM spike validates evaluator/profiler schemas against real Claude output BEFORE phases 4/5/6 are written. Spike gate is PROCEED / PATCH / ABORT. Runtime mitigation unchanged: Zero-Hallucination Contracts (LOD Pattern 7) retry up to 3x with schema feedback; if still failing, fall back to direct user prompt for manual verdict. | executor |
| R5 | **Profiler write conflicts** — two profiler invocations overlap and clobber each other's edits to `wiki/learner/*.md`. | 5, 6 | LOW | MED | Single-writer rule (mirrors base plugin wiki-maintainer exception at `raw-data/jaewon-plugin/docs/wiki/SCHEMA.md:109-113`); subagent-stop hook serializes profiler invocations via a status-file lock. | executor |
| R6 | **Sycophantic fallback** — teacher slips into supportive mode against spec §1 requirement ("sharp and strict, not sycophantic"). | runtime | HIGH | HIGH | **REV 2**: phase 0.5 spike checks that evaluator with `bar_adjustment='strict'` actually produces strict output and cites push_tactic_snapshot.tactic in evidence. Discuss-phase prompt in `learn` skill includes explicit anti-sycophancy contract; push-tactics.md rubric penalizes "encouragement without correction"; profiler flags sessions where verdict=mastery with <3 distinct challenges. | user (observation) / planner (rubric) |
| R7 | **120-LOC ceiling broken at runtime** — creator agent generates a read.md > 120 lines and wiki-maintainer has to split; cross-links can break. | 4, 5 | MED | LOW | **REV 2**: new read-header schema (phase-3 schemas/read-header.mjs) + wiki-maintainer "read-md gate" (phase-4.8) validate frontmatter + section headers before index linking. Creator prompt enforces split-on-generation (mirrors wiki-maintainer split protocol `raw-data/jaewon-plugin/docs/wiki/SCHEMA.md:33-39`); wiki-maintainer lint catches overruns in CI. | executor |
| R8 | **Merge conflicts on main** — two concurrent courses hit course/slug merges, no branching discipline. | 5, 6 | LOW | MED | Plan supports one active course at a time; `new-course` skill refuses to create a new branch if an active course exists with non-mastery verdict history. | executor |
| R9 | **Dashboard data leak** — publishing gh-pages exposes personal learner profile publicly. | 7 | MED | HIGH | Dashboard publish is opt-in (skill prompts explicit confirmation); `render-profile.mjs` has a `privacy_mode` flag that redacts `weaknesses` + `session-log` in published view; documented in `dashboard/README.md` (phase-7.8). | user (consent) / executor |
| R10 | **Smoke test drift from real-LLM behavior** — mocks pass but real agents produce output that fails validation. | 8 | HIGH | MED | **REV 2**: phase 0.5 spike catches this early for evaluator/profiler. Phase-8.5 real-LLM runbook must be executed once before v0.1 release; bugs found feed into v0.2. Risk acknowledged and bounded. | user (runbook) |
| R11 | **MCP tool clash with base plugin** — if user runs both `jaewon-plugin` and `jaewon-plugin-learning`, tool names could collide. | 3 | LOW | LOW | All learning tools prefixed `learning_` (phase-3); no overlap with base `jaewon_*` names verified against `raw-data/jaewon-plugin/mcp-server/handlers/status-handler.js:25`. | planner |
| R12 | **Git branch explosion** — many courses leave behind merged branches, cluttering `git branch -a`. | 4, 8 | LOW | LOW | `git-manager` deletes `course/<slug>` branches after successful merge (matches base policy at `raw-data/jaewon-plugin/agents/git-manager.md:78-80`). | executor |
| R13 | **Teacher-session context overflow** — long courses pump too much into systemMessage via learner-profile. | 6 | MED | MED | `summarizeForContext(profile, maxChars)` caps at 2KB (phase-0 §6.4); session-log truncated to last 20 entries before injection. | executor |
| R14 | **Researcher creates untrusted code** — git clone of arbitrary GitHub URL runs nothing but stores files under `wiki/courses/*/raw/`; risk is if user accidentally executes. | 4 | LOW | MED | `raw/` directory is read-only convention; researcher agent prompt forbids `npm install` or any execution; add `.gitignore` entry preventing accidental executable runs. | executor |
| R15 | **Feedback loop non-convergence** — repeated `partial` verdicts on the same chapter never escalate. | 5 | LOW | MED | `/verdict` skill bounds iterations to 3 (LOD Pattern 9); after 3, escalates to learner with explicit "adjust push-tactics.md" prompt (phase-5.6 step 5). | executor |
| R16 | **Stop/SubagentStop double-advance race** — both hooks fire on evaluator completion in undefined order; without a guard, `cycle_iteration` could be incremented twice for a single evaluator run. | 6 | MED | MED | **REV 2**: new `course_state.last_advance_sig` field + `state.advanceIfNewSig` compare-and-swap helper (phase-6.2); Stop and SubagentStop both compute the same sig and abort if it already matches. Tests 6.7#8 and 6.7#9 exercise both firing orders. | executor |
| R17 | **Phase 0.5 spike blocks phases 4-6 indefinitely** — real-LLM spike exposes a schema flaw that requires deep replan; all downstream work waits. | 0.5 | LOW | MED | **REV 2**: spike is bounded at 0.5 day + $5 API spend; explicit ABORT trigger in notes.md §Revision Log means the plan owner re-scopes instead of hand-wringing. Phases 1, 2, 3, 7 are independent of the spike outcome and can proceed in parallel. | planner (gate decision) |

## Top-3 Live Watchlist (for executor to pin)

1. **R3** (crawl latency) — **REV 2** now has code-level enforcement via `learning_crawl_guard`. Watchlist reason: real-world token estimation accuracy under the 50K budget is still untested until phase 0.5 runs.
2. **R6** (sycophantic fallback) — user's #1 stated requirement. Phase 0.5 gives first empirical signal; user observation remains primary detector.
3. **R10** (mock/real drift) — partially retired by R4's phase 0.5 mitigation; full retirement awaits the phase-8.5 real-LLM runbook run.
